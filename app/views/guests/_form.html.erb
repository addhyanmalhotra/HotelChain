
<%= simple_form_for(@guest) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :member_id, collection: @members || [["hi,1"]]%>
    <%= f.input :branch_id, collection: @branches || [["hi,1"]]%>
    <%= f.input :r_no, collection: @rooms %>
    <%= f.input :check_in %>
    <%= f.input :check_out %>
    <%= f.input :billed_amount %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
